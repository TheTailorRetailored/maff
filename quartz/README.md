# Maff Quartz Workspace

This directory is mounted into the API container. The API copies a workspace vault into `content/` and runs the Quartz build command when `rebuild_quartz_site` is triggered.

